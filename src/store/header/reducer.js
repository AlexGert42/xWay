const initialState = {
    ГЛАВНАЯ: "/fit/",
    УСЛУГИ: "/fit/",
    ОТЗЫВЫ: "/fit/",
    КОНТАКТЫ: "/fit/"
}

const headerReducer = (state = initialState, action) => {
    switch (action.type) {
        
    }


    return state
}